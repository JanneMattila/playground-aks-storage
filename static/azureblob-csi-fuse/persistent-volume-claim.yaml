apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blobfuse-pvc
  namespace: demos
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 100Gi
  volumeName: blob-csi-fuse
